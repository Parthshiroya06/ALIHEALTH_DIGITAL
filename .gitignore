# OSX
.DS_Store

# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
**/.xcode.env.local
ios/Pods/

# Android/IntelliJ
build/
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
*.keystore
!debug.keystore
# Release key: kept in this PRIVATE repo on purpose (never make the repo public)
!android/app/alihealth-release.keystore

# node.js
node_modules/
npm-debug.log
yarn-error.log

# Ruby / CocoaPods
/vendor/bundle/
**/Pods/

# Metro / testing
.metro-health-check*
/coverage

# Temporary files created by Metro to check the health of the file watcher
.watchman-*

# Yarn (berry)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vendor bracelet SDKs (shared under NDA – never commit)
android/app/libs/*.aar
android/app/libs/*.jar
# H Band iOS SDK frameworks (downloaded by `yarn sdk:hband:ios`)
modules/react-native-aliband-sdk/ios/Frameworks/

# Local project notes (client details – keep out of the public repo)
CLAUDE.md

# H Band SDK native libs (downloaded by `yarn sdk:hband`)
android/app/src/main/jniLibs/
