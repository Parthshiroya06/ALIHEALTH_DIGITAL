///
/// HybridAliBandSdkSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `BandInfo` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandInfo; }
// Forward declaration of `BandProfile` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandProfile; }
// Forward declaration of `MeasurementType` to properly resolve imports.
namespace margelo::nitro::alibandsdk { enum class MeasurementType; }
// Forward declaration of `BandSteps` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandSteps; }
// Forward declaration of `BandBattery` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandBattery; }
// Forward declaration of `BandReading` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandReading; }
// Forward declaration of `BandMeasurement` to properly resolve imports.
namespace margelo::nitro::alibandsdk { struct BandMeasurement; }

#include "BandInfo.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include "BandProfile.hpp"
#include "MeasurementType.hpp"
#include "BandSteps.hpp"
#include "BandBattery.hpp"
#include "BandReading.hpp"
#include <vector>
#include "BandMeasurement.hpp"
#include <functional>

namespace margelo::nitro::alibandsdk {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `AliBandSdk`
   * Inherit this class to create instances of `HybridAliBandSdkSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAliBandSdk: public HybridAliBandSdkSpec {
   * public:
   *   HybridAliBandSdk(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAliBandSdkSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAliBandSdkSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAliBandSdkSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<BandInfo>> connect(const std::string& mac, const std::string& password, const BandProfile& profile) = 0;
      virtual std::shared_ptr<Promise<void>> disconnect() = 0;
      virtual bool isConnected() = 0;
      virtual std::shared_ptr<Promise<void>> startMeasurement(MeasurementType type) = 0;
      virtual std::shared_ptr<Promise<void>> stopMeasurement(MeasurementType type) = 0;
      virtual std::shared_ptr<Promise<BandSteps>> readCurrentSteps() = 0;
      virtual std::shared_ptr<Promise<BandBattery>> readBattery() = 0;
      virtual std::shared_ptr<Promise<std::vector<BandReading>>> syncHistory() = 0;
      virtual void setOnMeasurement(const std::function<void(const BandMeasurement& /* event */)>& listener) = 0;
      virtual void setOnConnectionChange(const std::function<void(bool /* connected */)>& listener) = 0;
      virtual void setOnSyncProgress(const std::function<void(double /* progress */)>& listener) = 0;
      virtual void setOnLog(const std::function<void(const std::string& /* message */)>& listener) = 0;
      virtual void clearListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "AliBandSdk";
  };

} // namespace margelo::nitro::alibandsdk
